{#
  Category Overlay Board Component
  This component creates the overlay structure that will be populated
  dynamically by the overlay-board.js script
#}

<div id="category-overlay-board-container" style="display: none;">
    {# This container will be created dynamically by JavaScript #}
    {# No need for static HTML here as the overlay-board.js handles everything #}
</div>

{# 
  Alternative: If you want to provide a static fallback structure,
  uncomment the code below. However, the JavaScript creates this dynamically.
#}

{# 
<div id="overlay-backdrop" style="display: none;">
    <div id="category-overlay-board">
        <button id="overlay-close-btn" aria-label="{{ trans('common.close') }}">&times;</button>
        <div id="overlay-content">
            <!-- Content loaded dynamically via AJAX -->
        </div>
    </div>
</div>
#}

<style>
    /* Additional overlay-specific styles */
    #category-overlay-board {
        font-family: inherit;
    }
    
    #overlay-content h1,
    #overlay-content h2,
    #overlay-content h3 {
        color: var(--text-primary);
        margin-bottom: 1rem;
    }
    
    #overlay-content .products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 1.5rem;
        margin-top: 2rem;
    }
    
    #overlay-content .product-card {
        background: var(--bg-secondary);
        border: 1px solid var(--border-color);
        border-radius: 12px;
        overflow: hidden;
        transition: all 0.3s ease;
    }
    
    #overlay-content .product-card:hover {
        border-color: var(--violet-primary);
        transform: translateY(-4px);
        box-shadow: 0 10px 30px rgba(138, 43, 226, 0.2);
    }
    
    #overlay-content .product-card-image {
        position: relative;
        aspect-ratio: 1;
        overflow: hidden;
        background: var(--bg-tertiary);
    }
    
    #overlay-content .product-card-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
    }
    
    #overlay-content .product-card:hover .product-card-image img {
        transform: scale(1.1);
    }
    
    #overlay-content .product-card-content {
        padding: 1rem;
    }
    
    #overlay-content .product-card-title {
        font-size: 1rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: var(--text-primary);
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    
    #overlay-content .product-card-price {
        font-size: 1.3rem;
        font-weight: 700;
        color: var(--violet-primary);
        margin-bottom: 0.75rem;
    }
    
    #overlay-content .product-card-actions {
        display: flex;
        gap: 0.5rem;
    }
    
    #overlay-content .btn-add-to-cart {
        flex: 1;
        padding: 0.6rem 1rem;
        background: var(--violet-primary);
        color: white;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
    }
    
    #overlay-content .btn-add-to-cart:hover {
        background: var(--violet-hover);
        transform: translateY(-2px);
    }
    
    #overlay-content .category-description {
        margin: 2rem 0;
        padding: 1.5rem;
        background: rgba(138, 43, 226, 0.05);
        border-right: 4px solid var(--violet-primary);
        border-radius: 8px;
        color: var(--text-secondary);
        line-height: 1.6;
    }
    
    #overlay-content .category-header {
        margin-bottom: 2rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid var(--border-color);
    }
    
    #overlay-content .category-title {
        font-size: 2rem;
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: 0.5rem;
    }
    
    #overlay-content .category-count {
        color: var(--text-muted);
        font-size: 0.9rem;
    }
    
    /* Filters and Sorting */
    #overlay-content .filters-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
        gap: 1rem;
        flex-wrap: wrap;
    }
    
    #overlay-content .filter-group {
        display: flex;
        gap: 0.5rem;
        align-items: center;
    }
    
    #overlay-content .filter-label {
        font-weight: 500;
        color: var(--text-secondary);
    }
    
    #overlay-content select.filter-select {
        padding: 0.5rem 1rem;
        background: var(--bg-secondary);
        border: 1px solid var(--border-color);
        border-radius: 8px;
        color: var(--text-primary);
        cursor: pointer;
    }
    
    /* Pagination */
    #overlay-content .pagination {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 0.5rem;
        margin-top: 2rem;
    }
    
    #overlay-content .pagination a,
    #overlay-content .pagination span {
        padding: 0.5rem 1rem;
        background: var(--bg-secondary);
        border: 1px solid var(--border-color);
        border-radius: 8px;
        color: var(--text-primary);
        text-decoration: none;
        transition: all 0.3s ease;
    }
    
    #overlay-content .pagination a:hover {
        background: var(--violet-primary);
        border-color: var(--violet-primary);
    }
    
    #overlay-content .pagination .current {
        background: var(--violet-primary);
        border-color: var(--violet-primary);
        font-weight: 600;
    }
    
    /* Empty State */
    #overlay-content .empty-state {
        text-align: center;
        padding: 4rem 2rem;
    }
    
    #overlay-content .empty-state-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        opacity: 0.5;
    }
    
    #overlay-content .empty-state-title {
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
        color: var(--text-primary);
    }
    
    #overlay-content .empty-state-text {
        color: var(--text-muted);
    }
    
    /* Responsive Adjustments */
    @media (max-width: 768px) {
        #overlay-content .products-grid {
            grid-template-columns: 1fr;
            gap: 1rem;
        }
        
        #overlay-content .category-title {
            font-size: 1.5rem;
        }
        
        #overlay-content .filters-bar {
            flex-direction: column;
            align-items: stretch;
        }
        
        #overlay-content .filter-group {
            width: 100%;
            justify-content: space-between;
        }
        
        #overlay-content select.filter-select {
            flex: 1;
        }
    }
    
    @media (max-width: 480px) {
        #overlay-content {
            padding: 60px 20px 20px;
        }
    }
</style>
